{
  "entrypoint": "com.github.klainstom.${name.toLowerCase()}.ExtensionMain",
  "name": "${name}",
  "version": "${version}",
  "meta": {
    "github": {
      "release": {
        "account": "KlainStom",
        "repo": "terminus",
        "release": "v${version}"
      }
    }
  }
}